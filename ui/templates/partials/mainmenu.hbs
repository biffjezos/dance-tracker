            <nav class="main-menu">
                <button
                    id="main-menu-toggle"
                    class="menu-toggle-button d-lg-none"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#main-menu-items"
                    aria-expanded="false"
                    aria-controls="main-menu-items"
                > ☰ MENU </button>

                {{! Category buttons are rendered client-side (menu.js,
                    renderCategoryButtons) from the operations the engine
                    actually reports via get_operations() - never listed
                    here, so a category with nothing registered under it
                    just doesn't get a button. }}
                <div class="main-menu-items collapse d-lg-flex" id="main-menu-items"></div>
            </nav>
