Finish your current tas. When done, start working on tickets:

- [1.md](1.md)
- [2.md](2.md)
- [3.md](3.md)

Commit and push each step. Create a PR if all tasks of a TODO file are done.

When done, check if the following bugs still exist. If yes. Fix them:

1) OUTPUT: Cannot resize the OUTPUT. OUTPUT SIZE +|- Doesn't change the footage, screen, status bar size info
2) NODES: Visibility ON|OFF stops and plays the video, but it's not invisible. Please tell me if intended, if it's still needed. and if yes. fix it.
3) KEY: There is a default MASK 1. If no video loaded and I press the selector is says NO MASK YET. If a video loaded there's a default MASK 1. I told you many times. NO FUCKING DEFAULT MASKS. Now NO DEFAULT OPERATIONS.
4) KEY: If I select a source for the mask to VIDEO 1 and change the mode to CHROMA, the preview goes to MASK 1 turns pink and the colour picker doesn't work. Can't pick from VIDEO 1. no crosshair no video displayed.
5) Get rid of the Name LAYER EVERYWHERE. It's either the generic term NODE or specific MASK asf..
6) The RINGS in the preview canvas act crazy and jump around. In the output canvas the rings areok.
7) RING EDIT: I can select 8 STROKES. I HAVE ONLY 2. ONLY DISPLAY THE NODES I HAVE. NO FUCKING PHANTASY DEFAULTS. NEVER!!! NEVER! NEVER! NEVER! NEVER AGAIN. WRITE A FUCKING CLAUDE.MD 
8) Colour selection doesn't work for RINGS. 
9) Text renders on top of a video, although I have not told it do to it. There must be HARDWIRED COMPOSE operations.
10) DO NOT FUCKING USE ANY DEFAULTS OPERATIONS ON APP START, NO DEFAULT NODES. NO MASKS NEVER NEVER NEVER! If there's no BACKGROUND (Compose Operation) DO NOT LAY ANYTHING OVER ANYTHING IF NOT FUCKING EXPLICITLY DONE BY THE USER! TAKE THIS AND WRITE IT INTO YOUR FUCKING CLAUDE.MD STUPID MOTHER FUCKER.
11) Get rid of your stupid token wasting tests. Get rid of them. The app doesn't work, despite your testing crap.

